---
tags: Rockchip, 烧写
---
# rockchip uboot 进入烧写模式
1. 按键进入烧写模式：开机阶段插着 USB 的情况下长按 "音量+（adc 键）"；
2. 命令行进入烧写模式：
	* U-Boot 命令行输入 `rbrom` ：进入  **maskrom 烧写模式**；
	* U-Boot 命令行输入“rockusb 0 $devtype $devnum”：进入 **loader 烧写模式**, 如：
	```bash
	$ rockusb 0 mmc 0
	$ rockusb 0 # 查看使用范例
	```