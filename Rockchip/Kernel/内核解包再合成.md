---
tags: Rockchip/Kernel 
---


## `boot.img` 解压
```shell
./script/unpack_bootimg --boot_img boot.img --out out/
```

## `resource.img` 解压
```shell
./script/resource_tool --unpack --verbose --image=second out
```
- 从 `boot.img` 解压出来的 `resource.img` 被重命名为 `second`

