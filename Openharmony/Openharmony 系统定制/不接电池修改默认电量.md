---
tags:
  - OpenHarmony
---
## 没有充电 IC，默认电量修改
- 路径：`drivers/peripheral/battery/interfaces/hdi_service/src/power_supply_provider.cpp`
```diff
diff --git a/battery/interfaces/hdi_service/src/power_supply_provider.cpp b/battery/interfaces/hdi_service/src/power_supply_provider.cpp
index 3e89cf1d2..0588029b9 100644
--- a/battery/interfaces/hdi_service/src/power_supply_provider.cpp
+++ b/battery/interfaces/hdi_service/src/power_supply_provider.cpp
@@ -919,7 +919,7 @@ void PowerSupplyProvider::CreateMockChargerPath(std::string& mockChargerPath)
 void PowerSupplyProvider::CreateMockBatteryPath(std::string& mockBatteryPath)
 {
     BATTERY_HILOGI(FEATURE_BATT_INFO, "create mockFilePath path");
-    CreateFile(mockBatteryPath + "/capacity", "11");
+    CreateFile(mockBatteryPath + "/capacity", "50");
     CreateFile(mockBatteryPath + "/charge_control_limit", "0");
     CreateFile(mockBatteryPath + "/charge_counter", "4000000");
     CreateFile(mockBatteryPath + "/charge_full", "4000000")
```