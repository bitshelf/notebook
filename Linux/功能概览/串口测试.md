---
tags: Serial
---

# 串口
## picocom
```shell
sudo apt install picocom
```

#### 使用
```shell
sudo usermod -a -G dialout $USER
groups $USER
picocom -b 9600 /dev/ttyS7
```

## cutecom 图形化界面
```shell
sudo apt-get install cutecom
```