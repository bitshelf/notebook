---
tags: Linux/script 
---

# bash 数组
1. 数组定义
~~~shell
IPTS=（ 10.0.0.1 10.0.0.2 10.0.0.3 ）
~~~
* 括号与元素之间可以有空格
* 元素之间用空格分割

2. 显示数组所有元素
	*  `echo $(IPTS[@]}`
3. 显示数组
	* `echo ${#IPTS[@]}`
4. 显示数组的第一个元素
	* `echo ${IPTS[0]}`