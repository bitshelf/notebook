---
tags:
  - Bash
---
# 字符串测试运算符
* `-a` :  判断是否为空字符串，是空则为 true
* `-n` :  判断是否为非空字符串，非空则为 true
* `>`  :  从字典上看，如果 string1 排在 string2 之后，则为 true
* `<`  :  从字典上看，如果 string1 排在 string2 之前，则为 true
> [!tip] =~
> `=~` 的作用：测试 `=~` 左侧的字符串能否被右侧的 PATTERN 所匹配(左边的变量匹配到右边的正则表达式，则返回状态码 $0$，否则返回 $1$)
> 此表达式一般用于 `[[ ]]` 中
*  `${VALUE:-value}` 如果*VALUE*值为空，则值为*value*