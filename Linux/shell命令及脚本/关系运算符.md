---
tags:
  - Bash
---
## 关系运算符
1. `-gt`：大于，greater than。
2. `-eq`：等于，equal。 
3. `-lt`：小于，less than。
4. `-ge`：大于等于，greater than or equal。
5. `-le`：小于等于，less than or equal。
6. `-ne`：不等于，not equal。
## 连接符
1. `-a`：且，and。
2. `-o`：或，or。
## 条件判断
1. `&&`：用来执行条件成立后执行的命令
2. `||`：用来执行条件不成立后的执行命令