---
tags: Linux/script
---

# 括号
* `()` `(())` `$()` 圆括号
	* 单独使用圆括号会产生一个子 shell `(xyz=123)`
	* 数组初始化 `IPS=(ip1 ip2 ip3)`
* `[]` `[[]]` 方括号
	* 单独使用方括号是测试（test）或数组元素功能
	* 两个方括号表示测试表达式
* `<>` 尖括号：重定向符号
* `{}` 花括号
	* 输出范围 `echo {0..0}`
	* 文件复制 `cp /etc/passwd{,.bak}`
