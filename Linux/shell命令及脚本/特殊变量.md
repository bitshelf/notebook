---
tags:
  - Bash
---
# bash 特殊变量
* `!$`是一个特殊的环境变量,代表了上一个命令的最后一个字符串 
~~~shell
	$mkdir mydir
	$mv mydir yourdir
	$cd yourdir
~~~
可以改为
~~~shell 
	$mkdir mydir
	$mv !$ yourdir
	$cd !$
~~~
* `$_`上一个命令的最后一个字符串
* `$0` ： 代表脚本名称
* `$*` 和 `$@` 代表所有位置参数
* `$#` 代表位置参数的数量