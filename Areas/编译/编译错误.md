* `ERROR: Running build_buildroot failed!`
> 不要单独删除 `./buildroot/output/` 下某一个单独文件，要将整个 `./buildroot/output/` 目录删除
* `Error:<AddFile> open file failed, err=2!`
	> 1.  `git status` 查看文件状态
	2. 恢复被删除的 `recovery. img` 的文件
