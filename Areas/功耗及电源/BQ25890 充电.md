---
tags:
  - Power
---
## FUSB302 + BQ25890 + CW2015 快充调试
![](assets/BQ25890%20原理图.png)
> [!attention] 
> -  使用 RK 打过补丁的 BQ 25890 驱动
> - Type-C 供电（BQ25890 提供）与公板（GPIO 控制）不同，参考公板 dts 配置行不通

## 串口打印说明
```
插入鼠标，鼠标可以正常使用，会打印

[ 38.414902][ T1] init: processing action (sys.usb.config=adb && sys.usb.configfs=1 && sys.usb.ffs.ready=1) from (/system/etc/init/hw/init.usb.configfs.rc:21)  
[ 38.416555][ T1] init: Command 'symlink /config/usb_gadget/g1/functions/ffs.adb /config/usb_gadget/g1/configs/b.1/f1' action=sys.usb.config=adb && sys.usb.configfs=1 && sys.usb.ffs.ready=1 (/system/etc/init/hw/init.usb.configfs.rc:23) took 0ms and failed: symlink() failed: File exists  
[ 38.417138][ T1] init: Command 'write /config/usb_gadget/g1/UDC ${sys.usb.controller}' action=sys.usb.config=adb && sys.usb.configfs=1 && sys.usb.ffs.ready=1 (/system/etc/init/hw/init.usb.configfs.rc:24) took 0ms and failed: Unable to write to file '/config/usb_gadget/g1/UDC': Unable to write file contents: Device or resource busy
```
- log 是正常的，otg 口做 host 的时候，由于 adbd 服务还在，会有 log 打印的

```
拔出充电器，什么都不接的情况会打印
bq25890-charger 2-006a: INT_INPUT_CURRENT:8, INT_INPUT_VOLTAGE:18, INT_CHARGE_CURRENT:8
```
### 源码分析
- 源码打印位置为 `dev_info`，目前不清楚为什么要在不充电的状态下，进行电压、电流的限制
```c
  if(state->chrg_status == 0) {
	  // Input Current Limit
      bq25890_field_write(bq, field_id: F_IILIM, val: SDP_INPUT_CURRENT);
	  // Input Voltage Limit Offset
      bq25890_field_write(bq, field_id: F_VINDPM_OFS, val: SDP_INPUT_VOLTAGE);
	  // Fast Charge Current Limit
      bq25890_field_write(bq, field_id: F_ICHG, val: SDP_CHARGE_CURRENT);
      dev_info(dev: bq->dev, "INT_INPUT_CURRENT:%x, INT_INPUT_VOLTAGE:%x, INT_CHARGE_CURRENT:%x\n",
      SDP_CHARGE_CURRENT, SDP_INPUT_VOLTAGE, SDP_CHARGE_CURRENT);
  }
```

- `state->chrg_status == 0` 为不充电状态，可以由 491 行说明 
```c
...
437 enum bq25890_status {
438     STATUS_NOT_CHARGING,
439     STATUS_PRE_CHARGING,
440     STATUS_FAST_CHARGING,
441     STATUS_TERMINATION_DONE,
442 };
...
487     switch (psp) {
488     case POWER_SUPPLY_PROP_STATUS:
489         if (!state.online)
490             val->intval = POWER_SUPPLY_STATUS_DISCHARGING;
491         else if (state.chrg_status == STATUS_NOT_CHARGING)
492             val->intval = POWER_SUPPLY_STATUS_NOT_CHARGING;
493         else if (state.chrg_status == STATUS_PRE_CHARGING ||
494              state.chrg_status == STATUS_FAST_CHARGING)
495             val->intval = POWER_SUPPLY_STATUS_CHARGING;
496         else if (state.chrg_status == STATUS_TERMINATION_DONE)
497             val->intval = POWER_SUPPLY_STATUS_FULL;
498         else
499             val->intval = POWER_SUPPLY_STATUS_UNKNOWN;
500
501         break;
...
```


### 工作流程
- 通过 TCPM (Type-C Port Manager) 
- Linux-4.19/5.10 支持 TCPM 软件框架和 TCPCI 协议

## USB 配置说明

```c
/* 
 * USB2.0 PHY0注册typec orientation switch，
 * 用于与TCPM子系统交互，获取USB热拔插的信息
 */
 &u2phy0 {
        orientation-switch;
        status = "okay";

        port {
                #address-cells = <1>;
                #size-cells = <0>;
                u2phy0_orientation_switch: endpoint@0 {
                        reg = <0>;
                        remote-endpoint = <&usbc0_orien_sw>;
                };
        };
};
```

```c
 usbc0: fusb302@22 {
		compatible = "fcs,fusb302";
		reg = <0x22>;
		/* 根据  INT 实际连接的 GPIO 进行修改 */
		interrupt-parent = <&gpio3>;
		interrupts = <RK_PD0 IRQ_TYPE_LEVEL_LOW>;
		int-n-gpios = <&gpio3 RK_PD0 GPIO_ACTIVE_LOW>;
		pinctrl-names = "default";
		pinctrl-0 = <&usbc0_int>;
		// type-C 供电，根据原理图可知由 BQ25890 提供
		vbus-supply = <&vbus5v0_typec>;
		status = "okay";

		ports {
			#address-cells = <1>;
			#size-cells = <0>;

			port@0 {
				reg = <0>;
				usbc0_role_sw: endpoint@0 {
					remote-endpoint = <&dwc3_0_role_switch>;
				};
			};
		};

        usb_con: connector {
            compatible = "usb-c-connector";
            label = "USB-C";
            data-role = "dual";
            power-role = "dual";
            try-power-role = "sink";
            op-sink-microwatt = <1000000>;
            sink-pdos =
                <PDO_FIXED(5000, 3000, PDO_FIXED_USB_COMM)
                    PDO_FIXED(9000, 3000, PDO_FIXED_USB_COMM)
                    PDO_FIXED(12000, 3000, PDO_FIXED_USB_COMM)>;
            source-pdos =
                <PDO_FIXED(5000, 3000, PDO_FIXED_USB_COMM)>;

            ports {
                #address-cells = <1>;
                #size-cells = <0>;

                port@0 {
                    reg = <0>;
                    usbc0_orien_sw: endpoint {
                        remote-endpoint = <&u2phy0_orientation_switch>;
                    };
                };
            };

        };
    };
```

## BQ25890 配置说明

```c
    bq25890: charger@6a {
      compatible = "ti,bq25895", "ti,bq25890";
      status = "okay";
      reg = <0x6a>;
      extcon = <&u2phy0>;
      pinctrl-names = "default";
      pinctrl-0 = <&charger_ok>;
      interrupt-parent = <&gpio1>;
      interrupts = <RK_PA7 IRQ_TYPE_EDGE_FALLING>;
      otg-mode-en-gpios = <&gpio3 RK_PD5 GPIO_ACTIVE_HIGH>;
      ti,usb-charger-detection = <&usbc0>;


      ti,max-input-voltage = <12000000>;
      ti,max-input-current = <4000000>;//要设置大些

      ti,battery-regulation-voltage = <4200000>;
      ti,charge-current = <1600000>;
      ti,termination-current = <50000>;
      ti,precharge-current = <128000>;
      ti,minimum-sys-voltage = <3600000>;
      ti,boost-voltage = <5062000>;
      ti,boost-max-current = <2150000>;

      //ti,use-ilim-pin;
      ti,thermal-regulation-threshold = <110>;

      regulators {
              vbus5v0_typec: vbus5v0-typec {
                      regulator-compatible = "otg-vbus";
                      regulator-name = "vbus5v0_typec";
              };
      };
    };
```

## Type-C host/devices 切换逻辑
```c
// kernel-5.10/include/linux/usb/tcpm.h
   * @set_roles:  Called to set power and data roles
// kernel-5.10/drivers/usb/typec/tcpm/fusb302.c
static void init_tcpc_dev(struct tcpc_dev *fusb302_tcpc_dev)                                                                          │  init_tcpc_dev(struct tcpc_dev *
{
	...
      fusb302_tcpc_dev->set_roles = tcpm_set_roles;
	...
}
```

```c
// drivers/usb/typec/tcpm/tcpm.c
  1046 static int tcpm_set_roles(struct tcpm_port *port, bool attached,
  1047               enum typec_role role, enum typec_data_role data)
  1048 {
  1049     enum typec_orientation orientation;
  1050     enum usb_role usb_role;
  1051     int ret;
  1052
  1053     if (port->polarity == TYPEC_POLARITY_CC1)
  1054         orientation = TYPEC_ORIENTATION_NORMAL;
  1055     else
  1056         orientation = TYPEC_ORIENTATION_REVERSE;
  1057
  1058     if (port->typec_caps.data == TYPEC_PORT_DRD) {
  1059         if (data == TYPEC_HOST)
  1060             usb_role = USB_ROLE_HOST;
  1061         else
  1062             usb_role = USB_ROLE_DEVICE;
  1063     } else if (port->typec_caps.data == TYPEC_PORT_DFP) {
  1064         if (data == TYPEC_HOST) {
  1065             if (role == TYPEC_SOURCE)
  1066                 usb_role = USB_ROLE_HOST;
  1067             else
  1068                 usb_role = USB_ROLE_NONE;
```

```c
// drivers/usb/typec/tcpm/fusb302.c
static int tcpm_set_roles(struct tcpc_dev *dev, bool attached,
                enum typec_role pwr, enum typec_data_role data)
  {
      struct fusb302_chip *chip = container_of(dev, struct fusb302_chip,
                           tcpc_dev);                                                                                                                                    =
      int ret = 0;
      u8 switches1_mask = FUSB_REG_SWITCHES1_POWERROLE |
                  FUSB_REG_SWITCHES1_DATAROLE;
      u8 switches1_data = 0x00;

      mutex_lock(&chip->lock);
      if (pwr == TYPEC_SOURCE)                                                                                                                                           =
          switches1_data |= FUSB_REG_SWITCHES1_POWERROLE;
      if (data == TYPEC_HOST)  [N 9/9]
          switches1_data |= FUSB_REG_SWITCHES1_DATAROLE;
      ret = fusb302_i2c_mask_write(chip, FUSB_REG_SWITCHES1,
                       switches1_mask, switches1_data);
      if (ret < 0) {
          fusb302_log(chip, "unable to set pd header %s, %s, ret=%d",
                  typec_role_name[pwr], typec_data_role_name[data],
                  ret);
          goto done;
      }
```