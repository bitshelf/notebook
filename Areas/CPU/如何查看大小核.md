---
tags:
  - CPU
---
## ARM 查看大小核
```shell
cat /sys/devices/system/cpu/cpu*/cpu_capacity
```
- `cpu_capacity` 值高的就是大核，这是内核调度器用来决策的依据

## 查看 CPU 频率
```shell
cat /sys/devices/system/cpu/cpu*/cpufreq/cpuinfo_max_freq
```

## 查看 CPU 簇拓扑
```shell
cat /sys/devices/system/cpu/cpu*/topology/cluster_id
```