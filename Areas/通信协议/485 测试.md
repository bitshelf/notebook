---
tags: 485
---

## buildroot 485 测试
在两个板子分别输入
```shell
microcom -s 9600 /dev/ttyS6
```
- ?  先输入命令的板子作为发送设备，无法接收
- ? 输入字符时，有的字符显示与按键不对应
- ? 按 `<C-x>` 退出 microcom
### 现象
1. 在其中一个板子输入字符，在另一个板子看到字符的输出