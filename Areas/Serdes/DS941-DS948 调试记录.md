---
tags:
  - Serdes
---
## 硬件检查
在软件调试前，需确保硬件满足以下基本要求：
- 时钟信号：DS90UB941AS 支持 DSI 参考时钟、外部参考时钟和内部参考时钟模式。若使用 SoC 的 DSI 时钟作为参考，时钟必须是连续的（Continuous Clock Mode）
    
- 模式选择（MODE_SEL）：通过 ﻿MODE_SEL0﻿ 和 ﻿MODE_SEL1﻿ 引脚的外接分压电阻配置 DSI 通道数（1-4 Lane）、是否开启视频分割（Splitter Mode）、连接线类型（STP 或 Coax）以及时钟模式