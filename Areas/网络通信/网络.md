---
tags: Network buildroot
---

# 网络
## 网络故障排除工具
-   `ping`
-   `traceroute`
-   `mtr`
-   `nslookup`
-   `telnet`
-   `tcpdump`
-   `netstat`
-   `ss`

## rfkill
1. `echo 1 >/sys/class/rfkill/rfkill0/state` 使能指定网卡
> [!note]
> # buildroot 文件系统WiFi固件所在位置
> 1.`buildroot/board/rockchip/common/base/system/etc/firmware/`
> 
> 
## Ubuntu系统静态IP地址配置
```shell
# 配置静态IP文件/etc/network/interfaces
#vi /etc/network/interfaces
auto eth0
iface eth0 inet static
address 192.168.0.252
gateway 192.168.0.1
netmask 255.255.255.0
#network 192.168.0.0
#broadcast 192.168.0.255

// 重启网卡使配置生效
#/etc/init.d/networking restart
```

![nmcli 静态IP设置](../../ubuntu/nmcli%20静态IP设置.md)
## route 
1. `route del default`
2. `route add default dev ppp0`
# 路由表
```shell 
$ ip route
```
### 路由表编辑
#### 选项
```shell 
ipv4.gateway:
ipv4.routes: 
ipv4.route-metric:

ipv6.gateway:
ipv6.routes:
ipv6.route-metric:
```
#### 编辑命令
```shell 
$ nmcli connection modify <name> +ipv4.routes <destination> ipv4.gateway <gateway>
$ nmcli connection modify <name> ipv4.route-metric <metric>
```
可以通过 `nmcli c show`获取`<name>`

# 其他
1. `sudo ifmetric wlan0 50`
2. `udhcpc -i eth0`自动配置路由
3. **DNS** IP地址：$114.114.114.114$ 、$8.8.8.8$

# Link
- [How do I set up networking/WiFi/static IP address on Raspbian/Raspberry Pi OS? - Raspberry Pi Stack Exchange](https://raspberrypi.stackexchange.com/questions/37920/how-do-i-set-up-networking-wifi-static-ip-address-on-raspbian-raspberry-pi-os/37921#37921)